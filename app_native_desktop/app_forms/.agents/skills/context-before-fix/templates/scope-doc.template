# Scope Document — {Tên Tính Năng / Lỗi}

> **Date**: {YYYY-MM-DD}
> **Status**: Initial | Updated | Ready
> **Skill**: context-before-fix v1.0.0
> **Architecture**: C# WinForms Clean 3-Layer (0_Shared, 1_Backend, 2_Frontend)

---

## §1: Problem Summary

{Mô tả ngắn gọn vấn đề được báo cáo}

**Entry Point**: {file/class/service/component}
**Feature Area**: {module/screen name}
**Architecture Layer**: 0_Shared | 1_Backend | 2_Frontend

---

## §2: Scope Definition

### 2.1 Problem Area

- **Layer**: {0_Shared | 1_Backend | 2_Frontend}
- **Namespace / Sub-directory**: {namespace}
- **Component / Service**: {class/file}
- **Method / Event**: {method/event name}

### 2.2 Boundary

**In Scope**:
- {list of in-scope items}

**Out of Scope**:
- {list of out-of-scope items}

---

## §3: Impact Analysis

### 3.1 Direct Impact

| File | Line(s) | Layer | Issue Description |
| :--- | :--- | :--- | :--- |
| | | | |

### 3.2 Indirect Impact

| File | Relationship / Subscriber | Description |
| :--- | :--- | :--- |
| | | |

### 3.3 Contracts & Thread-Safety Impact

| Interface / Contract | Status | Thread-Safety (InvokeOnUI) | Notes |
| :--- | :--- | :--- | :--- |
| | | | |

---

## §4: Call Chain & Event Propagation

```mermaid
graph TD
    A[{Entry Point / Event Trigger}] --> B[State Hook / Orchestrator]
    B --> C[Backend Service]
    C --> D[Contracts / Shared Repository]
    B -.->|Event Emit| E[UI Components / Screen]
```

### Callers (Upstream)
- {file}:{method} — gọi entry point

### Callees (Downstream)
- {file}:{method} — được entry point gọi

### Event Listeners (UI / Observers)
- {component}:{handler} — lắng nghe state hook event

---

## §5: Data Flow

### 5.1 Input
- {data/input 1: ví dụ Clipboard text, Form input}

### 5.2 Output
- {data/output 1: ví dụ Converted string, OutputPreviewBox}

### 5.3 Dependencies
- {dependency 1: ví dụ IRoomCodeRepository, ISettingsService}

---

## §6: Affected Components

### 6.1 Files
```
• {file1}
• {file2}
• {file3}
```

### 6.2 Classes / Interfaces / Methods
```
• {file1}:{Class}.{Method}()
• {file2}:{IInterface}
• {file3}:{Component}.BindData()
```

### 6.3 Shared Data / Config
```
• {shared models / enum}
• {json storage / settings}
```

---

## §7: Evidence

<evidence>
<file>{file path}</file>
<line>{line number}</line>
<finding>{specific finding}</finding>
</evidence>

<evidence>
<file>{file path}</file>
<line>{line number}</line>
<finding>{specific finding}</finding>
</evidence>

---

## §8: Confidence Assessment

```yaml
overall_confidence: {X}%

breakdown:
  entry_point_identification: {X}%
  impact_mapping: {X}%
  call_chain_trace: {X}%
  evidence_verification: {X}%
```

### Uncertainty Flags
- {flag 1 — điều chưa hoàn toàn chắc chắn}
- {flag 2 — điểm cần tham vấn thêm}

---

## §9: Open Questions

| # | Question | Priority | Status |
| :--- | :--- | :--- | :--- |
| 1 | | High/Medium/Low | Open/Resolved |
| 2 | | High/Medium/Low | Open/Resolved |

---

## §10: Next Steps

{Hướng dẫn cho giai đoạn fix tiếp theo sau khi scope document được duyệt}

```
✓ Scope Context Document Complete
✓ NO Code Changes Made
✓ Document ready for fix phase
```

---

> **Document**: `Docs/context-to-work/{feature-name}/scope.{YYYY-MM-DD}.md`
> **Generated by**: context-before-fix v1.0.0
> **Language**: Vietnamese
