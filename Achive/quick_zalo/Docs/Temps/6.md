Edited 6.md
Viewed scope.2026-08-02.md:291-318
Viewed scope.2026-08-02.md:1-303
Viewed useDataNormalization.ts:1-45
Viewed normalized-listing-repository.port.ts:1-55

Dưới đây là phân tích chi tiết về **nguồn gốc vấn đề và định hướng xử lý** cho 6 câu hỏi mở trong đoạn văn bản **§10: Open Questions** thuộc file [scope.2026-08-02.md (data-normalization)](file:///home/stveve/Documents/workspace/Sales/extension/quick_zalo/docs/context-to-work/data-normalization-pattern/scope.2026-08-02.md#L304-L313):

---

### 1. Nguồn gốc Dead Code của `MessageDeduplicationService`
- **Nguồn gốc vấn đề**:
  - Ban đầu, `MessageDeduplicationService` tại [deduplication.service.ts](file:///home/stveve/Documents/workspace/Sales/extension/quick_zalo/src/domain/data-normalization/services/deduplication.service.ts#L14-L44) được thiết kế như một Domain Service chuẩn để xử lý khử trùng lặp dữ liệu Stage 1 trong RAM bằng bộ băm MD5/hash thô.
  - Tuy nhiên, khi cài đặt giao diện nạp file tại [useDataNormalization.ts:L74-L88](file:///home/stveve/Documents/workspace/Sales/extension/quick_zalo/src/features/data-normalization/hooks/useDataNormalization.ts#L74-L88), lập trình viên đã **tự viết lại (re-implement) logic đếm và lọc hash inline ngay bên trong React Hook** (`Set seenHashes + dupesInFile++`), khiến `MessageDeduplicationService` bị rớt lại thành Dead Code.
- **Định hướng**: **Không nên xóa**. Cần refactor để React Hook `useDataNormalization` gọi qua `MessageDeduplicationService`, loại bỏ code lặp inline và khôi phục đúng vai trò của Domain Service.

---

### 2. Sự lệch hợp đồng (Contract Drift) tại Adapter `dexie-normalized-listing.adapter.ts`
- **Nguồn gốc vấn đề**:
  - Port [normalized-listing-repository.port.ts:L39](file:///home/stveve/Documents/workspace/Sales/extension/quick_zalo/src/app/ports/normalized-listing-repository.port.ts#L39) định nghĩa `saveBatch(listings, dupesInFileCount)` nhận 2 tham số và trả về `SaveListingBatchResult`.
  - Trong quá trình phát triển tính năng "Nạp file chuẩn hóa JSON", phát sinh nhu cầu đo lường thống kê chi tiết theo Template (`IngestionMetricsEx`) và ghi nhật ký nạp (`ImportSession`). Tác giả Adapter [dexie-normalized-listing.adapter.ts](file:///home/stveve/Documents/workspace/Sales/extension/quick_zalo/src/infra/storage/dexie-normalized-listing.adapter.ts#L135-L139) đã mở rộng chữ ký hàm thành 3 tham số, ép kiểu `Result<any>`, và viết bổ sung method `getLatestSession()`, nhưng **bỏ quên việc cập nhật đồng bộ ngược lại Port Contract**.
- **Định hướng**: **Chính thức mở rộng Port**. Cần cập nhật `INormalizedListingRepository` (hoặc tạo thêm port cho Session) phản ánh đúng nhu cầu nghiệp vụ thực tế, đồng thời loại bỏ kiểu `any` để đảm bảo Type Safety.

---

### 3. Nguyên nhân `useDataNormalization` tự `new` Adapter/Service & Hướng khắc phục DI
- **Nguồn gốc vấn đề**:
  - Tại [useDataNormalization.ts:L8-L9](file:///home/stveve/Documents/workspace/Sales/extension/quick_zalo/src/features/data-normalization/hooks/useDataNormalization.ts#L6-L9), Hook khởi tạo trực tiếp:
    ```ts
    const normalizer = new DataNormalizationService();
    const repo = new DexieNormalizedListingRepository();
    ```
  - Lập trình viên đã chọn giải pháp "viết nhanh" (Short-cut) bằng cách import trực tiếp Concrete Class từ tầng `@infra` vào tầng `@features`, bỏ qua Container Dependency Injection của hệ thống.
- **Định hướng**: Cần tạo `data-normalization.container.ts` nằm trong thư mục `src/composition/` (tương tự pattern của `quick-search.container.ts`) để chịu trách nhiệm khởi tạo và inject các Port Instance vào Feature Hook.

---

### 4. Bất đồng bộ tài liệu: `tree_work.md:130` ↔ `ui-architecture-conventions.md:12`
- **Nguồn gốc vấn đề**:
  - [tree_work.md:L130](file:///home/stveve/Documents/workspace/Sales/extension/quick_zalo/Docs/tree_work.md#L130) liệt kê dòng `features/*/ui`, ngụ ý cho phép cấu trúc thư mục chứa subfolder `ui/`.
  - Trong khi đó, [.agents/rules/ui-architecture-conventions.md:L12](file:///home/stveve/Documents/workspace/Sales/extension/quick_zalo/.agents/rules/ui-architecture-conventions.md#L12) lại đặt quy chuẩn cấm lồng thư mục `ui/` thừa (`must_not: features/feature-name/ui/...`), bắt buộc dùng cấu trúc phẳng (`Flat Feature Structure`).
- **Định hướng**: **`ui-architecture-conventions.md` là Single Source of Truth**. Cần đưa file màn hình `DataNormalizationScreen.tsx` ra root của thư mục `src/features/data-normalization/` và cập nhật lại `tree_work.md` để khớp với quy chuẩn chính thức.

---

### 5. Thư mục ma `quick-search/` tại `tree_work.md:89`
- **Nguồn gốc vấn đề**:
  - [tree_work.md:L89](file:///home/stveve/Documents/workspace/Sales/extension/quick_zalo/Docs/tree_work.md#L89) đăng ký trước sự tồn tại của `features/quick-search/`, nhưng thực tế thư mục này chưa được tạo trong `src/features/`. Đây là kết quả của việc lập kế hoạch kiến trúc trước khi triển khai code.
- **Định hướng**: Giữ nguyên đăng ký trong `tree_work.md` và tiến hành tạo thư mục `src/features/quick-search/` khi thực hiện nhiệm vụ Wiring cho module Quick Search.

---

### 6. Đồng bộ hóa khi sửa F4 cho cả 2 Module (`data-normalization` & `message-extraction`)
- **Nguồn gốc vấn đề**:
  - Cả 2 module đều bị công cụ tạo doc tự động (`module-docs-generation-skill`) đánh dấu WARN F4 một cách không chính xác (do skill không nhận diện đúng mô hình Feature-First).
- **Định hướng**: Khi tiến hành chuẩn hóa lại cấu trúc thư mục và tài liệu, nên thực hiện **đồng bộ trên cả 2 module** để đảm bảo tính nhất quán 100% cho toàn bộ cây thư mục `src/features/`.

---
