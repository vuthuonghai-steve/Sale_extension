import React from 'react';

/** Options = shell tĩnh (D7 Phase 5) — tree §4 không liệt kê options-app. */
export function OptionsApp(): React.JSX.Element {
  return (
    <div style={{ padding: 16, fontFamily: 'system-ui, sans-serif' }}>
      <h1 style={{ fontSize: 18 }}>Options</h1>
      <p>Đầy đủ settings sẽ đến cùng feature thật (Phase 6+).</p>
    </div>
  );
}
